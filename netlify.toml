[build]
  command = "npm run build"
  publish = "dist"

# Proxy API ke backend Node.js (isi URL production setelah deploy backend)
# [[redirects]]
#   from = "/api/*"
#   to = "https://api-myeconomy.onrender.com/:splat"
#   status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
